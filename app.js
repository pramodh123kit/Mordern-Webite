const menu = document.getElementById("btn");
const menuLinks = document.getElementById("light");

menu.addEventListener('click',function(){
  btn.classList.toggle("active");
  light.classList.toggle("on");
})
